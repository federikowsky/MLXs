# Backlog

Live deferred items only. Closed or blocked families belong in [FREEZE.md](FREEZE.md).

## Deferred acceptance candidates

- Additional reference model breadth only if M01 fails to move AC1 on current broadened set.
  - `mlx-community/Qwen2.5-3B-Instruct-4bit`
  - `mlx-community/Llama-3.1-8B-Instruct-4bit`
  - `mlx-community/Qwen2.5-7B-Instruct-4bit`

## Deferred technical families

- Revisit `AC13` only after structural redesign changes prefill/runtime ownership.
- Revisit generic long prepared-step only with fresh cross-model guardrail evidence.
- Revisit broad batch-aware cache contract only with a design that avoids the proven `256` catastrophe.

## Deferred product-ready work

- AC3 configuration final sweep.
- AC14 speculative decoding finish-out.
- AC15 tool-calling final closure.
- AC18 conversion pipeline closure.

## Parked ideas

- Dedicated redesign dossier file only if M01 archaeology becomes too large for WORKLOG + milestone files.
